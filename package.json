{
  "name": "ceo-mcp-media",
  "version": "1.0.0",
  "private": true,
  "description": "Durable provider-agnostic MCP media pipeline for image, video, audio and composition.",
  "type": "module",
  "engines": { "node": ">=20" },
  "bin": {
    "ceo-mcp-media": "./dist/src/mcp/server.js",
    "ceo-mcp-media-cli": "./dist/src/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/tests/job-store.test.js dist/tests/storyboard.test.js dist/tests/router.test.js dist/tests/audio-jobs.test.js dist/tests/video-resume.test.js dist/tests/gemini-payload.test.js",
    "start": "node dist/src/mcp/server.js",
    "dev": "tsx src/mcp/server.ts",
    "cli": "tsx src/cli/index.ts",
    "worker": "tsx src/cli/index.ts worker",
    "smoke:ffmpeg": "npm run build && node scripts/smoke-ffmpeg.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
